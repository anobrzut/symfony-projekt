{% extends 'base.html.twig' %}

{% block title %}Delete Tag{% endblock %}

{% block body %}
    <h1>Delete Tag</h1>

    {{ form_start(form) }}
    <p>Are you sure you want to delete this tag?</p>
    <button class="btn">Yes, delete</button>
    {{ form_end(form) }}

    <a href="{{ path('tag_index') }}">No, go back to list</a>
{% endblock %}
